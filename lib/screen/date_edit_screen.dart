import 'package:flutter/material.dart';

class Date_Edit extends StatelessWidget {
  const Date_Edit({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('date edit'),
    );
  }
}
